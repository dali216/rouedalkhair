# Roued Al Khair Platform

Version 1 — landing page, login UI and volunteer dashboard prototype.

## Cloudflare Pages
Production branch: `main`
Framework: None
Build command: empty / None
Build output directory: `/`

## Supabase
The UI is ready to be connected to Supabase Auth + Database. Do not put a service_role key in frontend files. Use the publishable/anon key only in browser-side code.
